#include<stdio.h>
#include<math.h>
#include<stdbool.h>

void main()
{
    int N;
    printf( "\nEnter the exponent of 2" );
    scanf( "%d", &N );

    // Just check the nearest (lower) multiple of 9 as the sum of digits cycles in mod 9.

}
